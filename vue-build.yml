version: '3'

services:
  npm:
    image: node:8-alpine
    working_dir: /vue
    env_file:
      - .env
    volumes:
      - ./docker/scripts/vue-build.sh:/scripts/vue-build.sh
      - ./vue/:/vue/
    command: /scripts/vue-build.sh
